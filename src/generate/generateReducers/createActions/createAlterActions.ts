import { ModelPath } from "../createPaths/types";
import { Tree } from "../../../createTree/types";
import { ReducerGenAction } from "../types";

export function createAlterActions(paths: ModelPath[], tree: Tree): ReducerGenAction[] {
  return [];
}

